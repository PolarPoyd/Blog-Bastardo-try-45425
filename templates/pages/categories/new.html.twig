{% extends 'base.html.twig' %}

{% block title %}
Nouvelle catégorie
{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="mt-3">Création d'une catégorie</h1>

        {{ form_start(form) }}

        <div class="form-group">
            {{ form_label(form.name) }}
            {{ form_widget(form.name) }}
                <div class="form-error">
                    {{ form_errors(form.name) }}
                </div>
        </div>

            <div class="d-flex justify-content-start gap-3">
                <a href="{{ path('categories.index') }}" class="btn btn-primary align-self-end">
                    Retour
                </a>
        <div class="form-group">
            {{ form_row(form.submit) }}
        </div>

        <div>
        {{ form_end(form) }}
        </div>
            </div>
    </div>
{% endblock %}